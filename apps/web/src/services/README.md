# Services Directory (`src/services`)

Contains application-level services (API clients, integration services, authentication abstractions).

Business logic for specific modules belongs inside `src/features/<feature-name>/services/`.
