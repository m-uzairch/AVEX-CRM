# Custom React Hooks (`src/hooks`)

This directory contains application-wide, reusable React hooks (e.g., `useDebounce`, `useMediaQuery`, `usePagination`).

Feature-specific hooks belong inside their corresponding feature directory in `src/features/<feature-name>/hooks/`.
